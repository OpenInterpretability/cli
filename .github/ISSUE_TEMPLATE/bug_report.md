---
name: 🐛 Bug report
about: Something in the openinterp package is broken
labels: ["bug"]
---

**Minimal repro**
```python
# paste a minimal example that fails
```

**Expected** vs **actual**:

**Environment**
- `openinterp --version`:
- `python --version`:
- OS:
- Installed with `pip install openinterp` or `openinterp[full]`?

**Traceback** (if any)
```
paste here
```
